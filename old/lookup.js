/* eslint-env node, mocha */
'use strict';

const { expect } = require('chai');
const findMatch = require('../lib/lookup');
const formats = require('../data/formats');

describe('findMatch', () => {
  it('Should load');
  it('Should match a single-part signature');
  it('Should match a multi-part signature');
});
